# Test Dataset

This directory contains the test split of the Cosmetic Surgery Dataset.

The test set is kept separate from training and validation data
and is used for final model evaluation.

The same subject must not appear in more than one dataset split.

Large image files are excluded from GitHub.
